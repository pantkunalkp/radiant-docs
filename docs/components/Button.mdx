---
sidebar_position: 6
---

import PropsTable from "@site/src/utils/PropsTable";

# Button

A button, when clicked or tapped triggers an action or a series of actions.

Below are the relevant attributes for all button types.

### Sizes

Buttons are available in 3 sizes: `sm`, `md` (default) and `lg`

The hierarchy for text size in button labels is:

- **H4** for large buttons
- **H5** for medium buttons
- **H6** for small buttons

```jsx live
function ButtonSizes() {
  return (
    <div>
        <StyledBox>
          <Button size="lg">Large</Button>
          <Button size="md">Medium</Button>
          <Button size="sm">Small</Button>
        </StyledBox>
    </div>
  );
}
```

### Variants

Buttons are available in 4 global variants: `solid` (default), `soft`, `outlined` and `plain`

```jsx live
function ButtonVariants() {
  return (
    <div>
      <CssVarsProvider>
        <StyledBox>
          <Button variant="solid">Solid</Button>
          <Button variant="soft">Soft</Button>
          <Button variant="outlined">Outlined</Button>
          <Button variant="plain">Plain</Button>
        </StyledBox>
      </CssVarsProvider>
    </div>
  );
}
```

### Buttons with icons

Buttons do have startIcon and endIcon props

```jsx live
function ButtonVariants() {
  return (
    <div>
      <CssVarsProvider>
        <StyledBox>
          <Button
            size="sm"
            startIcon={
              <ChipDelete variant="plain" sx={{ width: 16, height: 16 }} />
            }
            variant="outlined"
          >
            This is Button
          </Button>
          <Button
            size="sm"
            endIcon={
              <ChipDelete variant="plain" sx={{ width: 16, height: 16 }} />
            }
            variant="outlined"
          >
            This is Button
          </Button>
          <Button
            size="sm"
            endIcon={
              <ChipDelete variant="plain" sx={{ width: 16, height: 16 }} />
            }
            startIcon={
              <ChipDelete variant="plain" sx={{ width: 16, height: 16 }} />
            }
            variant="outlined"
          >
            This is Button
          </Button>
        </StyledBox>
      </CssVarsProvider>
    </div>
  );
}
```

### Colors

All components do support 6 color type: `primary` (default), `neutral`, `danger`, `info`, `success` and `warning`

```jsx live
function ButtonVariants() {
  return (
    <div>
      <CssVarsProvider>
        <StyledBox>
          <Button color="primary">primary</Button>
          <Button color="neutral">neutral</Button>
          <Button color="danger">danger</Button>
          <Button color="info">info</Button>
          <Button color="success">success</Button>
          <Button color="warning">warning</Button>
        </StyledBox>
      </CssVarsProvider>
    </div>
  );
}
```

## Props

<PropsTable name="Button" />
